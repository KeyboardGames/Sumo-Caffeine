#ifndef _Complex_INCLUDE
#define _Complex_INCLUDE

extern std::string pathA, pathM, modelPS, texturePS, imageNS, freeIS;
extern double xLS, yLS, zLS;
extern int nowA, loopA, loopB, loopC, loopD, loopE, interestH;
extern double top, sideA, sideB, sideC, sideD, bottom;
extern double storedA, storedB, storedC, storedD, storedE, storedF;
extern double averageY, volumeA;

void iSaveTAXI();

#endif
